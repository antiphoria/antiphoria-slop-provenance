# Python-focused org quality gate template.
name: Org Quality (Python)

on:
  pull_request:
  push:
    branches:
      - $default-branch
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality-gate:
    uses: antiphoria/.github/.github/workflows/quality-gate.yml@main
    with:
      policy-ref: main
      run-trivy: true
      run-python-ruff: true
      run-python-test: true
      run-python-codeaudit: true
      run-node-quality: false
      run-ci-handoff: true
